name: CI

# Every workflow here is called by other repositories, so a syntax error or a
# bad expression ships straight into a consumer's production ops run. Lint on
# the way in. actionlint also runs shellcheck over every `run:` body.

on:
  push:
    branches: [main]
    # Release tags run CI too, so `version-sweep` can assert the tag being cut
    # matches what the tree is stamped with (see that job).
    tags: ['v*.*.*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: actionlint + shellcheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: raven-actions/actionlint@3d39aea434753780c3b3d4a1a31c854b4dbf49d7 # v2.2.0
        with:
          fail-on-error: true

  catalog:
    name: catalog.json is current
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # catalog.json is what a consuming repo's agent reads instead of parsing
      # ~7,500 lines of workflow YAML. It is generated, so the only way it stays
      # true is failing here when someone changes a contract without regenerating.
      - run: pip install --quiet pyyaml
      - run: python3 scripts/gen_catalog.py --check

  version-sweep:
    name: version stamps and doc pins agree
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Two failure modes, one script. On a PR this only asserts the tree agrees
      # with ITSELF, so an unrelated PR is never failed by a release happening.
      # On a tag push it additionally asserts the tree agrees with the TAG — the
      # moment a skipped sweep would otherwise ship docs pinned to the previous
      # release and workflows stamped with a version nobody can use.
      - name: Check the sweep
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            python3 scripts/stamp_version.py --check --expect "${{ github.ref_name }}"
          else
            python3 scripts/stamp_version.py --check
          fi

      - name: Run stamp-sweep tests
        run: python3 tests/run_stamp_tests.py

  doc-examples:
    name: doc examples are valid workflows
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Every ```yaml block in docs/ that looks like a complete workflow is
      # extracted and linted. v1.20.0 shipped a copy-paste example that failed at
      # startup for anyone who used it; nothing caught it because the example was
      # prose, not code. Now it is code.
      - name: Extract complete workflow examples
        run: |
          mkdir -p /tmp/examples/.github/workflows
          python3 - <<'PY'
          import re, pathlib
          out = pathlib.Path("/tmp/examples/.github/workflows")
          n = 0
          for md in sorted(pathlib.Path("docs").glob("*.md")) + [pathlib.Path("README.md")]:
              for i, block in enumerate(re.findall(r"```yaml\n(.*?)```", md.read_text(), re.S)):
                  # A complete workflow has jobs and a trigger; fragments ("with:" only) do not.
                  if re.search(r"^jobs:", block, re.M) and re.search(r"^on:", block, re.M):
                      (out / f"{md.stem}-{i}.yml").write_text(block)
                      n += 1
          print(f"extracted {n} complete example workflow(s)")
          if n == 0:
              raise SystemExit("::error::no complete examples found — the extractor is broken")
          PY

      - uses: raven-actions/actionlint@3d39aea434753780c3b3d4a1a31c854b4dbf49d7 # v2.2.0
        with:
          files: /tmp/examples/.github/workflows/*.yml
          fail-on-error: true

  plan-fixtures:
    name: cleanup-gar-images delete-plan fixtures
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # actionlint's shellcheck does not descend into `<<'PY'` heredocs, so the
      # delete-plan algorithm is otherwise entirely unlinted and untested — while
      # being the thing that decides what gets permanently deleted from GAR.
      # The runner extracts that heredoc from the workflow and executes it, so
      # this exercises the shipped code rather than a copy that can drift.
      - name: Run delete-plan fixtures
        run: python3 tests/run_plan_tests.py

  step-bodies:
    name: step bodies (stubbed commands)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # shellcheck lints these bodies but never runs them — which is how the
      # deploy-cloud-run Summary bug shipped (TODO.md). This extracts a step's
      # `run:` script from the workflow and executes it against stubbed commands
      # and a fake clock, so retry/backoff logic is asserted, not assumed.
      - name: Run step-body tests
        run: |
          python3 -m pip install --quiet --disable-pip-version-check pyyaml
          python3 tests/run_step_tests.py

  pinned-actions:
    name: third-party actions are SHA-pinned
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Reject mutable action refs
        run: |
          # Matches `uses: owner/repo@ref` where ref is not a 40-char hex SHA.
          # Local (`./...`) and reusable-workflow refs are excluded by `[^./]`.
          if grep -rnP '^\s*(-\s*)?uses:\s*[^./][^@]+@(?![0-9a-f]{40}\s*($|#))' \
               --include='*.yml' --include='*.yaml' .github/workflows; then
            echo "::error::Third-party actions must be pinned to a full commit SHA."
            exit 1
          fi
          echo "All third-party action references are SHA-pinned."
